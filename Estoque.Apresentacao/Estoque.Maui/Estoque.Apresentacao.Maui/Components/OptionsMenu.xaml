<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Estoque.Apresentacao.Maui.Components.OptionsMenu"
             Title="OptionsMenu">
    <VerticalStackLayout Padding="20" Spacing="10" Background="{StaticResource Primary}">

        <Label 
            Margin="0, 10, 0, 0"
            Text="MENU" 
            FontSize="24" 
            HorizontalOptions="Center" 
            TextColor="White" />

        <Button 
            Text="Estoque" 
            TextColor="White"
            BackgroundColor="{StaticResource PrimaryDark}"/>

        <Button 
            Text="Cadastros" 
            TextColor="White"
            BackgroundColor="{StaticResource PrimaryDark}"/>

        <Button 
            Text="Cadastros" 
            TextColor="White"
            BackgroundColor="{StaticResource PrimaryDark}"/>

        <Button 
            Text="Cadastros" 
            TextColor="White"
            BackgroundColor="{StaticResource PrimaryDark}"/>


        <Button 
            Text="Cadastros" 
            TextColor="White"
            BackgroundColor="{StaticResource PrimaryDark}"/>
        
    </VerticalStackLayout>
</ContentPage>